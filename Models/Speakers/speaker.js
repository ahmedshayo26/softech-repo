class speaker {
    constructor(size, type, model, manufacterer){
        this.size = size,
        this.type = type,
        this.model = model,
        this.manufacterer = manufacterer
    }
}

module.exports = speaker